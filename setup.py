from setuptools import setup, find_packages

setup(
    name="fraud_detection",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # List your dependencies here if needed, e.g.:
        # "numpy",
        # "pandas",
        # "scikit-learn",
        # "fastapi",
        # "uvicorn",
        # "python-dotenv",
    ],
)